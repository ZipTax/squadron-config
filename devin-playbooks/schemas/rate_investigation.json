{
  "type": "object",
  "properties": {
    "outcome": {
      "type": "string",
      "enum": [
        "completed",
        "needs_human"
      ],
      "description": "Whether this lane completed its bounded work. Use needs_human only when the lane cannot reach a supported result without information or a decision that only a person can provide; non-blocking follow-up questions do not change a completed outcome."
    },
    "human_questions": {
      "type": "array",
      "description": "Questions that prevent this lane from completing. Empty when outcome is completed. Squadron decides who receives them and where work resumes.",
      "items": {
        "type": "object",
        "properties": {
          "question": { "type": "string" },
          "context": {
            "type": "string",
            "description": "Facts the session already knows that the person needs in order to answer the question."
          }
        },
        "required": ["question", "context"],
        "additionalProperties": false
      }
    },
    "verdict": {
      "type": "string",
      "enum": [
        "DEFECT_PROVEN",
        "WORKING_AS_INTENDED",
        "EVIDENCE_INCOMPLETE"
      ],
      "description": "Routing verdict. Exactly one of the three enum values; the orchestrator routes on this field, not on the summary prose."
    },
    "question_matched_verdict": {
      "type": "string",
      "description": "The verdict in the terms the ticket asked (e.g. 'Discrepancy explained', 'No discrepancy'), plus how it maps to the routing verdict above."
    },
    "evidence_complete": {
      "type": "boolean",
      "description": "True only if every load-bearing claim is measured or traced. False forces EVIDENCE_INCOMPLETE."
    },
    "working_as_intended": {
      "type": "boolean",
      "description": "True only with positive evidence: the correct value decomposed, or a condition that precludes the reported behavior. An absent reproduction is not evidence."
    },
    "disposition": {
      "type": "string",
      "enum": [
        "data/configuration change",
        "procedure/function change",
        "both",
        "unsupported at available granularity",
        ""
      ],
      "description": "The remedy the evidence supports, empty when the verdict is not DEFECT_PROVEN. 'both' is common and not a hedge: rate rows that are wrong AND applied wrongly need the migration and the proc change. 'unsupported at available granularity' means the legacy model cannot express the fix, which is a limitation to record, not a fix to attempt."
    },
    "disposition_detail": {
      "type": "string",
      "description": "The remedy in specifics: the object to change, or the table and rows, or the mechanism class that cannot be expressed. On 'unsupported', whether a narrower partial fix is feasible AND accurate for every address it would cover -- not merely for the ticket's address."
    },
    "gates": {
      "type": "object",
      "properties": {
        "question_match": {
          "type": "string",
          "description": "pass | fail | n/a -- the verdict answers the question the ticket asked."
        },
        "basis": {
          "type": "string",
          "description": "pass | fail | n/a -- every load-bearing claim is measured or traced."
        },
        "mechanism_located": {
          "type": "string",
          "description": "pass | fail | n/a -- the mechanism is pinned to the object(s)/symbol(s) and the input that reaches them, not left as a symptom. Several sites in one object is fine; a single line is not required."
        },
        "alternative_killed": {
          "type": "string",
          "description": "pass | fail | n/a -- the competing explanations were ruled out by evidence, not by preference."
        },
        "scope_stated": {
          "type": "string",
          "description": "pass | fail | n/a -- who else is affected: merchants, states, periods, surfaces."
        }
      },
      "required": [
        "question_match",
        "basis",
        "mechanism_located",
        "alternative_killed",
        "scope_stated"
      ],
      "additionalProperties": false
    },
    "mechanism": {
      "type": "string",
      "description": "What is wrong and where expected and actual part ways — the object(s)/symbol(s), the input that reaches them, and both values. May legitimately be several sites in one object rather than a single line. Mirrors the mission's `mechanism` field."
    },
    "affected_roots": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Execution roots the defect reaches (e.g. spTransactionLookup, spImportOfflineTransactions, fnGetTaxRatesforTx_nonssuta). Drives case coverage downstream."
    },
    "paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Surfaces affected: cart, import, reports/filing."
    },
    "evidence": {
      "type": "array",
      "description": "Every load-bearing claim with its basis and citation.",
      "items": {
        "type": "object",
        "properties": {
          "claim": {
            "type": "string",
            "description": "The claim."
          },
          "basis": {
            "type": "string",
            "enum": [
              "measured",
              "traced",
              "inferred",
              "hedge"
            ],
            "description": "measured = a query result (snapshot facts are measured for that dated copy only); traced = read out of code or state-published material; inferred/hedge cannot support a terminal verdict."
          },
          "citation": {
            "type": "string",
            "description": "The query, file path, capture, or published source."
          }
        },
        "required": [
          "claim",
          "basis",
          "citation"
        ],
        "additionalProperties": false
      }
    },
    "production_evidence": {
      "type": "array",
      "description": "Ticket-scoped observations from governed production data. Empty when production was not queried; snapshot evidence belongs in evidence instead.",
      "items": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "sufficient",
              "insufficient",
              "unavailable"
            ]
          },
          "ticket_gap_outcome": {
            "type": "string",
            "enum": [
              "none",
              "needs_human",
              "insufficient_production_evidence"
            ]
          },
          "claim": { "type": "string" },
          "observed_at": { "type": "string" },
          "catalog_objects": {
            "type": "array",
            "items": { "type": "string" }
          },
          "query_reference": { "type": "string" },
          "scope": {
            "type": "object",
            "properties": {
              "identifiers": {
                "type": "array",
                "items": { "type": "string" }
              },
              "date_from": { "type": "string" },
              "date_to": { "type": "string" }
            },
            "required": [
              "identifiers",
              "date_from",
              "date_to"
            ],
            "additionalProperties": false
          },
          "conclusion": { "type": "string" },
          "limitations": {
            "type": "array",
            "items": { "type": "string" }
          },
          "ticket_gap_reason": { "type": "string" }
        },
        "required": [
          "status",
          "ticket_gap_outcome",
          "claim",
          "observed_at",
          "catalog_objects",
          "query_reference",
          "scope",
          "conclusion",
          "limitations",
          "ticket_gap_reason"
        ],
        "additionalProperties": false
      }
    },
    "limitation_class": {
      "type": "string",
      "description": "The limitations.md entry (a ticket carrying the new-rate-engine label) this matches and the data proving the match, or 'none'. A match is a hypothesis proven from data, never a shortcut past cause analysis. Mirrors the mission's `limitation_class` field."
    },
    "unknowns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "What remains unknown, and what would resolve it."
    },
    "investigation_summary": {
      "type": "string",
      "description": "At most 150 words, product-level."
    }
  },
  "required": [
    "outcome",
    "human_questions",
    "verdict",
    "question_matched_verdict",
    "evidence_complete",
    "working_as_intended",
    "disposition",
    "mechanism",
    "gates",
    "affected_roots",
    "paths",
    "evidence",
    "production_evidence",
    "unknowns",
    "investigation_summary"
  ],
  "additionalProperties": false
}
